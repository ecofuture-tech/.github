name: Build and Publish (Reusable)

on:
  workflow_call:
    inputs:
      python-version:
        description: 'Python version to use'
        required: false
        type: string
        default: '3.12'
      working-directory:
        description: 'Working directory for the package'
        required: false
        type: string
        default: '.'
      publish-to-testpypi:
        description: 'Publish to Test PyPI for pre-releases'
        required: false
        type: boolean
        default: true
      create-github-release:
        description: 'Create GitHub release'
        required: false
        type: boolean
        default: true
    secrets:
      PYPI_TOKEN:
        description: 'PyPI API token'
        required: true
      TEST_PYPI_TOKEN:
        description: 'Test PyPI API token'
        required: false

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Extract package name from repository
      id: package
      run: |
        REPO_NAME="${GITHUB_REPOSITORY#*/}"
        echo "package_name=$REPO_NAME" >> $GITHUB_OUTPUT
        echo "✓ Package name: $REPO_NAME"

    - name: Install uv
      uses: astral-sh/setup-uv@v7
      with:
        enable-cache: true
        cache-dependency-glob: "uv.lock"

    - name: Set up Python
      run: uv python install ${{ inputs.python-version }}
    
    - name: Extract version from tag
      id: version
      run: |
        TAG=${GITHUB_REF#refs/tags/}
        echo "tag=$TAG" >> $GITHUB_OUTPUT
        
        # v1.0.0, v1.0.0-rc1, v1.0.0-beta1, v1.0.0-alpha1
        if [[ $TAG =~ ^v([0-9]+\.[0-9]+\.[0-9]+.*)$ ]]; then
          VERSION=${BASH_REMATCH[1]}
        else
          echo "❌ Invalid tag format: $TAG"
          echo "Expected format: v1.0.0, v1.0.0-rc1, v1.0.0-beta1, etc."
          exit 1
        fi
        
        echo "version=$VERSION" >> $GITHUB_OUTPUT
        echo "✓ Building version: $VERSION from tag: $TAG"
        
        if [[ $VERSION =~ (alpha|beta|rc) ]]; then
          echo "is_prerelease=true" >> $GITHUB_OUTPUT
          echo "📦 Pre-release detected"
        else
          echo "is_prerelease=false" >> $GITHUB_OUTPUT
          echo "🚀 Stable release detected"
        fi

    - name: Build package
      working-directory: ${{ inputs.working-directory }}
      run: |
        echo "Building package..."
        uv build
        echo "✓ Package built successfully"
        
        echo ""
        echo "Built artifacts:"
        ls -lh dist/

    - name: Publish to Test PyPI
      if: ${{ inputs.publish-to-testpypi && steps.version.outputs.is_prerelease == 'true' }}
      working-directory: ${{ inputs.working-directory }}
      run: |
        echo "Publishing to Test PyPI..."
        uv publish --token ${{ secrets.TEST_PYPI_TOKEN }} --publish-url https://test.pypi.org/legacy/
        echo "✓ Published to Test PyPI"
    
    - name: Publish to PyPI
      if: ${{ steps.version.outputs.is_prerelease == 'false' }}
      working-directory: ${{ inputs.working-directory }}
      run: |
        echo "Publishing to PyPI..."
        uv publish --token ${{ secrets.PYPI_TOKEN }}
        echo "✓ Published to PyPI"
    
    - name: Create GitHub Release
      if: ${{ inputs.create-github-release }}
      uses: softprops/action-gh-release@v2
      with:
        files: ${{ inputs.working-directory }}/dist/*
        generate_release_notes: true
        draft: false
        prerelease: ${{ steps.version.outputs.is_prerelease == 'true' }}
        name: Release ${{ steps.version.outputs.version }}
        body: |
          ## 📦 Release ${{ steps.version.outputs.version }}
          
          ### Installation
          ```bash
          uv add ${{ steps.package.outputs.package_name }}==${{ steps.version.outputs.version }}
          ```
          
          ### What's Changed
          
          See the full changelog below.
    
    - name: Summary
      if: always()
      run: |
        echo "### Build and Publish Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "- **Package:** ${{ steps.package.outputs.package_name }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Version:** ${{ steps.version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Tag:** ${{ steps.version.outputs.tag }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Pre-release:** ${{ steps.version.outputs.is_prerelease }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Status:** ${{ job.status == 'success' && '✅ Success' || '❌ Failed' }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        if [ "${{ job.status }}" = "success" ]; then
          if [ "${{ steps.version.outputs.is_prerelease }}" = "true" ]; then
            echo "📦 Published to Test PyPI: https://test.pypi.org/project/${{ steps.package.outputs.package_name }}/" >> $GITHUB_STEP_SUMMARY
          else
            echo "🚀 Published to PyPI: https://pypi.org/project/${{ steps.package.outputs.package_name }}/" >> $GITHUB_STEP_SUMMARY
          fi
        fi